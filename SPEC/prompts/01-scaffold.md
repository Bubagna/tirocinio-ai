STEP 1 — Scaffold

Crea lo scaffold del progetto in APP/ con:
- backend NodeJS (Express) in APP/server
- frontend Vite+React in APP/web
- una root README con comandi
- configurazione env (file .env.example, niente segreti hardcoded)

Aggiungi script npm root in APP/package.json per:
- dev (avvia backend+frontend)
- start (production)
- test (placeholder per ora)

Alla fine dammi:
- elenco file creati
- comandi per avviare dev
- cosa devo vedere (healthcheck)
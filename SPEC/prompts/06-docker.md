STEP 6 — Docker Compose

Crea docker compose con:
- mongo
- backend
- frontend (o un single container se preferisci, ma KISS)

Requisiti:
- deve bastare: docker compose up --build
- env gestite via .env (non committare segreti)

Alla fine:
- dimmi i comandi e le porte
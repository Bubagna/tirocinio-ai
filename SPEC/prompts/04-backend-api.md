STEP 4 — Backend API per dashboard

Implementa endpoint:
- GET /api/summary
- GET /api/users/top?limit=20
- GET /api/trends

I dati devono arrivare da Mongo (normalized).
Se manca informazione, usa fallback dal raw.

Alla fine:
- dimmi esempi di risposta JSON per ciascun endpoint
- dimmi comandi curl per testare
